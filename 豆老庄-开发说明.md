# 豆老庄｜开发说明（MVP）

## 1. 技术路线

建议采用 **微信小程序原生开发（TypeScript）+ NestJS + PostgreSQL**。

- **微信小程序原生开发：** 首期只面向小程序，使用原生页面、组件和 TypeScript，避免 H5/PC 适配与跨端抽象成本；后续需要 Web 时再抽取 API 类型和业务规则。
- **NestJS：** 提供 REST API、认证、业务事务、异步图纸转换任务，并通过适配层调用已具备初步实现的图纸转换算法项目。
- **PostgreSQL：** 适合库存流水、并发扣减与多表统计；关键写操作放入数据库事务。
- **对象存储：** 用于原始图片、图纸封面与生成网格图（腾讯云 COS/阿里云 OSS 均可），数据库只存元数据和 URL/对象键。
- **Redis（可选）：** 登录验证码、短期缓存、导入任务队列；MVP 用户量低时可后置。

推荐目录：

```text
doulaozhuang/
├── apps/
│   ├── miniprogram/     # 微信小程序原生页面、组件与状态管理
│   └── api/             # NestJS API
├── packages/
│   ├── shared-types/    # DTO、枚举、校验规则
│   ├── ui/              # 小程序基础组件、设计令牌
│   ├── mard-palette/    # Mard 221 色标准色卡、版本和查询工具
│   └── converter-client/# 对接既有图纸转换算法的客户端与 DTO
├── infra/               # Docker、部署配置、数据库迁移
└── docs/
```

## 2. 小程序端实现边界

| 能力 | 首期实现 |
| --- | --- |
| 图纸与库存浏览 | 小程序卡片/列表展示；按页面下拉刷新，不做 Web 管理后台 |
| 库存编辑 | 色卡选择器、逐项库存录入与数字步进器；首期不做 PC 表格批量编辑 |
| 图片导入与转换 | 使用 `wx.chooseMedia` 选择 PNG、JPG、JPEG、WebP，上传后创建异步转换任务；不支持 CSV/PDF |
| 图纸预览 | 原图与生成网格图预览，调用小程序图片预览能力 |
| 登录 | `wx.login` 换取业务 token；服务端统一签发会话 |
| 离线体验 | 缓存只读数据；MVP 不做离线写入队列，网络失败明确提示重试 |

## 3. 数据模型

### 3.1 核心表

| 表 | 关键字段 | 用途 |
| --- | --- | --- |
| `users` | id, nickname, avatar_url | 用户资料 |
| `auth_identities` | user_id, provider, provider_uid | 微信小程序登录身份绑定 |
| `mard_colors` | code, name, hex, palette_version, sort_order | 内置 Mard 221 色标准色卡，只读 |
| `beads` | id, user_id, mard_color_code, stock_qty, warning_qty, status | 用户豆子主数据；颜色外键指向 `mard_colors` |
| `patterns` | id, user_id, name, source_type, source_key, generated_key, status, grid_width, grid_height, total_planned_qty, version | 图纸及其原图、生成图纸元数据 |
| `pattern_colors` | id, pattern_id, mard_color_code, planned_qty | 图纸每种 Mard 颜色及计划用量 |
| `conversion_jobs` | id, user_id, source_key, source_type, status, request_json, result_json, error_code, algorithm_version | 图片转换任务与算法版本追溯 |
| `craft_sessions` | id, pattern_id, status, started_at, completed_at | 一次制作过程；MVP 每图纸默认一条 |
| `craft_usages` | id, session_id, pattern_color_id, actual_qty | 实际使用量 |
| `inventory_reservations` | id, bead_id, session_id, qty, status | 制作中的预占数量 |
| `inventory_transactions` | id, bead_id, type, qty_delta, reference_type, reference_id, balance_after | 不可变库存流水 |
| `shopping_items` | id, user_id, bead_id nullable, mard_color_code, suggested_qty, purchased_qty, status | 补豆项 |
| `pattern_files` | id, pattern_id, file_key, mime_type, size | 图纸附件 |

`beads.stock_qty` 是为查询性能保留的当前余额字段；任何修改必须同时写入 `inventory_transactions`，并由同一个数据库事务提交。

### 3.2 关键索引与约束

- `mard_colors(code)`：主键；色卡通过数据迁移一次性导入 221 条标准色。
- `beads(user_id, mard_color_code)`：唯一约束，确保每位用户对一个 Mard 色号只有一条有效库存。
- `patterns(user_id, status, updated_at desc)`：图纸库列表。
- `pattern_colors(pattern_id)`、`inventory_reservations(bead_id, status)`：用量和可用库存计算。
- `inventory_transactions(bead_id, created_at desc)`：库存流水追溯。
- 数量字段使用整数；禁止浮点数。

## 4. API 草案

接口统一前缀 `/api/v1`，写接口需要登录；所有响应使用 ISO 8601 时间和整数数量。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/mard-colors` | 查询内置 Mard 221 色卡，支持色号/色名搜索 |
| `GET/POST` | `/beads` | 查询/新增豆子；新增只能传 `mard_color_code`，查询支持颜色、低库存、排序 |
| `GET/PATCH` | `/beads/:id` | 豆子详情与编辑 |
| `POST` | `/beads/:id/adjustments` | 手动盘点，写入流水 |
| `GET` | `/patterns` | 图纸列表 |
| `GET/PATCH` | `/patterns/:id` | 图纸详情与编辑 |
| `POST` | `/patterns/conversions` | 上传完成后创建图片转换任务，传 `source_key`、来源类型、预设尺寸 |
| `GET` | `/patterns/conversions/:jobId` | 查询转换进度、失败原因或预览结果 |
| `POST` | `/patterns/conversions/:jobId/confirm` | 确认转换结果，创建图纸、用量表和生成图 |
| `POST` | `/patterns/:id/start` | 创建制作会话并预占库存 |
| `PATCH` | `/patterns/:id/usage` | 保存实际用豆草稿 |
| `POST` | `/patterns/:id/complete` | 原子确认消耗并完成 |
| `POST` | `/patterns/:id/cancel-session` | 释放未确认预占 |
| `GET` | `/shopping-items` | 获取按颜色合并后的补豆清单 |
| `PATCH` | `/shopping-items/:id` | 修改建议数量、购买状态 |
| `POST` | `/uploads/sign` | 获取附件直传签名 |

## 5. 关键事务与并发控制

### 开始制作

1. 锁定本图纸相关 `beads` 行（`SELECT ... FOR UPDATE`）。
2. 计算每色可用量：`stock_qty - 所有有效 reservation.qty 之和`。
3. 对可用量充足部分创建 `inventory_reservations`；不足部分返回缺口，但仍允许开始制作。
4. 创建/更新补豆项；提交事务。

### 完成制作

1. 锁定会话、库存记录和预占记录，校验会话仍为进行中。
2. 以实际用量为准扣减库存，写入 `inventory_transactions(type='consume')`；不得扣至负数。
3. 将预占状态改为已结算；未使用部分释放。
4. 更新图纸与会话状态为已完成；提交事务。

所有变更接口传入 `Idempotency-Key`。服务端按“用户 + 路径 + key”保存短期结果，避免弱网重试造成重复扣库存。

## 6. 图片转图纸规范与算法集成

### 6.1 输入与输出

- 输入只接受 PNG、JPG、JPEG、WebP 图片；首期拒绝 CSV、PDF 和手工色表。
- 上传来源由客户端标记为 `source_image`（原始图片）或 `existing_pattern_image`（已有拼豆图纸图片），用于算法采用不同的识别策略与产品统计，不改变用户流程。
- 输出必须包括：生成的网格图文件、格数（宽/高）、Mard 221 色用量表、总用豆数、实际使用的 Mard 色号集合和算法版本。
- 用户在确认页可改名称、选择预设格数并发起重新转换；确认前的结果不得进入图纸库和库存计算。

### 6.2 与现有算法项目的集成契约

豆老庄不重写转换算法，而是封装 `converter-client` 调用既有项目。算法服务应提供稳定的异步契约：

1. `POST /convert`：接收源图片的短期签名 URL、来源类型、目标格数/预设和 `palette=mard-221`，返回 `job_id`。
2. `GET /convert/:job_id`：返回 `queued/processing/succeeded/failed`，成功时包含生成图 URL、网格尺寸、按 `mard_color_code` 聚合的用量和 `algorithm_version`。
3. 若算法支持回调，服务端校验签名后更新 `conversion_jobs`；否则由队列定时轮询。客户端只轮询豆老庄 API，不直接访问算法服务。

转换结果进入数据库前必须校验：颜色数均属于内置 `mard_colors`、每色用量为正整数、总量等于各色用量之和、生成图和结果属于发起任务的用户。

### 6.3 异步流程与失败处理

1. 客户端调用 `/uploads/sign` 直传图片，再提交转换任务。
2. API 创建 `conversion_jobs(status='queued')`，将任务投递给算法适配层。
3. 转换完成后，服务端保存预览与结构化用量；客户端显示确认页。
4. 用户确认后才创建 `patterns` 与 `pattern_colors`，图纸初始状态为 `待拼`。
5. 转换失败时保留原图和错误码，用户可调整预设格数后重试；同一个幂等键不得创建重复任务。

## 7. 前端交互与设计要求

- 以豆子色块作为主识别元素，但每个色块必须同时显示文字名称/色号，保证可访问性和色盲可用性。
- 所有影响库存的按钮展示明确结果，例如“开始拼（预占 235 颗）”“确认完成并扣减”。
- 数量输入采用数字键盘与步进器；保存后显示本次变更和新余额。
- 图纸详情优先显示“当前缺口”和“可用库存”，而非只展示总库存。
- 页面必须处理空状态、加载状态、无网络、无权限、导入错误与并发冲突。

## 8. 安全、可观测性与质量

- 文件直传使用短期签名 URL；服务端校验 MIME、大小、对象归属，不允许客户端任意访问对象键。
- 按 `user_id` 做数据权限隔离，所有详情更新均在查询层附带归属条件。
- 日志记录请求 ID、用户 ID、图纸 ID、库存流水 ID；避免写入 token 与附件私有 URL。
- 错误监控覆盖：转换失败、库存事务冲突、文件上传失败、微信登录失败。
- 单元测试重点：Mard 色卡校验、库存可用量计算、预占/释放、完成扣减、幂等重试、转换结果校验。
- 集成测试重点：算法服务成功、超时、失败、回调重复与色卡外颜色返回。
- 端到端测试重点：选择 Mard 颜色并建库存 → 上传图片 → 转换确认 → 开始拼 → 缺豆 → 入库 → 完成；运行微信开发者工具与真机测试。

## 9. 交付节奏建议

| 阶段 | 内容 | 验证结果 |
| --- | --- | --- |
| 第 1 周 | 小程序工程、账号、数据库迁移、豆子库 CRUD | 可登录并维护库存 |
| 第 2 周 | Mard 221 色卡、图片上传、算法适配与转换确认页 | 能由图片生成可确认的 Mard 图纸 |
| 第 3 周 | 图纸库、制作状态、预占与流水 | 库存不重复分配、可追溯 |
| 第 4 周 | 补豆清单、微信小程序适配、测试与监控 | 完成 MVP 闭环验收 |

上线前需准备：微信小程序 AppID 与类目/隐私配置、业务域名和下载域名白名单、对象存储访问控制、数据库备份、生产错误监控和用户反馈入口。
